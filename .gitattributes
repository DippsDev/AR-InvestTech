# Normalize all text files to LF in the repository and on checkout, so Git
# stops warning about CRLF/LF conversion on a mixed Windows/Unix repo.
* text=auto eol=lf

*.png binary
*.jpg binary
*.ico binary
